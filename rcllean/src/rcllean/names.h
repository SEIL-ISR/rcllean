// Validating topic, node and namespace names against rcl's and rmw's rules,
// and expanding a name against a node.  Every entry point here is reached only
// from Lean, so nothing is declared for other files.

#ifndef RCLLEAN_NAMES_H
#define RCLLEAN_NAMES_H

#endif // RCLLEAN_NAMES_H
